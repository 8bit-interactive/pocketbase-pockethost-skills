name: Deploy to Pockethost

on:
  push:
    branches:
      - main
      - master
      - staging
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    # Prefer a release tag or commit SHA instead of @main once published.
    uses: 8bit-interactive/pocketbase-pockethost-skills/.github/workflows/pockethost-deploy.yml@main
    secrets: inherit
